@EndUserText.label: 'Access Control for ZI_RAP_Travel_Mode'
@MappingRole: true
define role ZI_RAP_TRAVEL_MODE {
    grant
        select
            on
                ZI_RAP_TRAVEL_MODE
                    where
                        // ( TravelStatus ) = aspect pfcg_auth( ZOSTATMode, ZOSTATMode, actvt = '03' )
                        // and
                        // CurrencyCode = 'EUR';
                        true;                        
}